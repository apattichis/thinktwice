# ThinkTwice Environment Configuration
# Copy this file to .env and fill in your API keys

# ===========================================
# REQUIRED - Anthropic API Key
# ===========================================
# Powers all reasoning steps in the pipeline
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ===========================================
# OPTIONAL - Search APIs for Fact Checking
# ===========================================
# At least one search API is recommended for web-based fact verification
# Without a search key, the system falls back to AI knowledge only

# Brave Search API (Primary)
# Free tier: 2,000 queries/month
# Get your key at: https://brave.com/search/api/
BRAVE_SEARCH_API_KEY=

# Tavily API (Fallback)
# Free tier: 1,000 queries/month
# Get your key at: https://tavily.com
TAVILY_API_KEY=

# ===========================================
# PIPELINE CONFIGURATION
# ===========================================
# These settings control the ThinkTwice self-correcting pipeline behavior.
# Defaults work well for most use cases.

# Gate confidence threshold (0-100). Drafts above this skip refinement.
# GATE_THRESHOLD=85

# Min sub-question pass rate for gate (0.0-1.0). 1.0 = all must pass.
# GATE_MIN_PASS_RATE=1.0

# Max refinement loop iterations (1-10).
# MAX_ITERATIONS=3

# Convergence confidence threshold (0-100).
# CONVERGENCE_THRESHOLD=80

# Enable self-verification track (true/false).
# SELF_VERIFY_ENABLED=true

# Run web + self verification in parallel (true/false).
# SELF_VERIFY_PARALLEL=true

# Enable trust comparison between draft and refined (true/false).
# TRUST_BLEND_ENABLED=true

# ===========================================
# SERVER CONFIGURATION
# ===========================================
HOST=0.0.0.0
PORT=8000
FRONTEND_URL=http://localhost:5173
